{
  "name": "duckflux",
  "version": "0.6.0",
  "type": "module",
  "description": "Declarative YAML-based workflow runner — CLI wrapper",
  "bin": {
    "duckflux": "bin/duckflux.js"
  },
  "files": [
    "bin"
  ],
  "scripts": {
    "build": "echo 'No build step for cli wrapper'"
  },
  "dependencies": {
    "@duckflux/runner": "workspace:*"
  }
}
