{
  "name": "duckflux",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun --filter '*' test",
    "build": "bun --filter '*' build",
    "compile": "bun run --cwd packages/runner compile"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/node": "^25.3.5",
    "testcontainers": "^11.13.0",
    "typescript": "^5.2.2"
  }
}
